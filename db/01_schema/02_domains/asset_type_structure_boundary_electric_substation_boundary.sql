-- TABLE: asset_type_structure_boundary_electric_substation_boundary
CREATE TABLE IF NOT EXISTS domains.asset_type_structure_boundary_electric_substation_boundary (
    code smallint PRIMARY KEY,
    description varchar(64),
    details varchar(2000)
);

