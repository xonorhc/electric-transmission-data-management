-- TABLE: electric_combined_bank_power_units
CREATE TABLE IF NOT EXISTS domains.electric_combined_bank_power_units (
    code smallint PRIMARY KEY,
    description varchar(64),
    details varchar(2000)
);

