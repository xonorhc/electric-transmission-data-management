-- TABLE: asset_type_electrans_line_high_voltage_connector
CREATE TABLE IF NOT EXISTS domains.asset_type_electrans_line_high_voltage_connector (
    code smallint PRIMARY KEY,
    description varchar(64),
    details varchar(2000)
);

