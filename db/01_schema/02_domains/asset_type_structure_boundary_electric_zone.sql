-- TABLE: asset_type_structure_boundary_electric_zone
CREATE TABLE IF NOT EXISTS domains.asset_type_structure_boundary_electric_zone (
    code smallint PRIMARY KEY,
    description varchar(64),
    details varchar(2000)
);

