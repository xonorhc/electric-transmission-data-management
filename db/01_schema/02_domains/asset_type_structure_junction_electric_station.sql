-- TABLE: asset_type_structure_junction_electric_station
CREATE TABLE IF NOT EXISTS domains.asset_type_structure_junction_electric_station (
    code smallint PRIMARY KEY,
    description varchar(64),
    details varchar(2000)
);

