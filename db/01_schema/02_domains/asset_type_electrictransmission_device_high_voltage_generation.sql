-- TABLE: asset_type_electrans_device_high_voltage_generation
CREATE TABLE IF NOT EXISTS domains.asset_type_electrans_device_high_voltage_generation (
    code smallint PRIMARY KEY,
    description varchar(64),
    details varchar(2000)
);

