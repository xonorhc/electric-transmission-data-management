-- TABLE: asset_type_electrans_junction_high_voltage_attachment
CREATE TABLE IF NOT EXISTS domains.asset_type_electrans_junction_high_voltage_attachment (
    code smallint PRIMARY KEY,
    description varchar(64),
    details varchar(2000)
);

