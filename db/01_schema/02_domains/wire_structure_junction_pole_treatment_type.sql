-- TABLE: wire_structure_junction_pole_treatment_type
CREATE TABLE IF NOT EXISTS domains.wire_structure_junction_pole_treatment_type (
    code smallint PRIMARY KEY,
    description varchar(64),
    details varchar(2000)
);

