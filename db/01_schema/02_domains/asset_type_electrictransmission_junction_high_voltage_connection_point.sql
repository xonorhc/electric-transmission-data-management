-- TABLE: asset_type_electrans_junction_high_voltage_connection_point
CREATE TABLE IF NOT EXISTS domains.asset_type_electrans_junction_high_voltage_connection_point (
    code smallint PRIMARY KEY,
    description varchar(64),
    details varchar(2000)
);

