-- TABLE: asset_type_electrans_line_high_voltage_busbar
CREATE TABLE IF NOT EXISTS domains.asset_type_electrans_line_high_voltage_busbar (
    code smallint PRIMARY KEY,
    description varchar(64),
    details varchar(2000)
);

