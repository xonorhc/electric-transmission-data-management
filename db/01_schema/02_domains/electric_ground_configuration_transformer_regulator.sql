-- TABLE: electric_ground_configuration_transformer_regulator
CREATE TABLE IF NOT EXISTS domains.electric_ground_configuration_transformer_regulator (
    code smallint PRIMARY KEY,
    description varchar(64),
    details varchar(2000)
);

