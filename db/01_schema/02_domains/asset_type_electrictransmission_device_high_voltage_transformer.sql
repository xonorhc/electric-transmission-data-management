-- TABLE: asset_type_electrans_device_high_voltage_transformer
CREATE TABLE IF NOT EXISTS domains.asset_type_electrans_device_high_voltage_transformer (
    code smallint PRIMARY KEY,
    description varchar(64),
    details varchar(2000)
);

