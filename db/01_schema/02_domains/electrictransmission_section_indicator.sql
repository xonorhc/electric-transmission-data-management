-- TABLE: electrictransmission_section_indicator
CREATE TABLE IF NOT EXISTS domains.electrictransmission_section_indicator (
    code smallint PRIMARY KEY,
    description varchar(64),
    details varchar(2000)
);

