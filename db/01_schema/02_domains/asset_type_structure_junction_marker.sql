-- TABLE: asset_type_structure_junction_marker
CREATE TABLE IF NOT EXISTS domains.asset_type_structure_junction_marker (
    code smallint PRIMARY KEY,
    description varchar(64),
    details varchar(2000)
);

