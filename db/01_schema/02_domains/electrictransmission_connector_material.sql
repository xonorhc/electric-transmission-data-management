-- TABLE: electrictransmission_connector_material
CREATE TABLE IF NOT EXISTS domains.electrictransmission_connector_material (
    code smallint PRIMARY KEY,
    description varchar(64),
    details varchar(2000)
);

